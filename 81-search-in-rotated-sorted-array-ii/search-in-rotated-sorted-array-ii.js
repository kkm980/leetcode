/**
 * @param {number[]} nums
 * @param {number} target
 * @return {boolean}
 */
var search = function(nums, target) {
    if(nums.indexOf(target)===-1){
        return false
    } else{
        return true;
    }
};